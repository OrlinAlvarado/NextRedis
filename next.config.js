/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.hondahonduras.com', 'carplanet.mx', 'www.placervial.com'],
  },
}

module.exports = nextConfig
